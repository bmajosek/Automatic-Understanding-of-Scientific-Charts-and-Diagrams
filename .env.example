# Copy this file to .env and add credentials only to that local copy.
# Never place real credentials in this example file.
GEMINI_API_KEY=
HF_TOKEN=
# Gemini model selection is configured in config/config.yaml.
# Omit TESSERACT_CMD when the executable is already available on PATH.
TESSERACT_CMD=C:\\Program Files\\Tesseract-OCR\\tesseract.exe
